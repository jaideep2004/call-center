# Live Test Report — 2026-09-07 — coveragecalls.com

**Date:** 2026-09-07 14:36 IST · **Site:** `https://coveragecalls.com` · **Cert valid:** Sep 4 17:26:48 → Dec 3 17:26:47 · **Issuer:** Let's Encrypt YE1

## Scoreboard

| # | Check | Result |
|---|---|---|
| 1.1 | `GET /api/v1/health` | ✅ 200 (2.83s) |
| 1.2 | `GET /api/telephony/mock/webhook` | ✅ 200 (3.39s) — `{"ok":true,"message":"Webhook endpoint ready (use POST)"}` |
| 1.3 | `GET /api/v1/cms?slug=faq` | ✅ 200 (3.74s) |
| 1.4 | `GET /api/v1/cms?slug=privacy` | ✅ 200 (4.14s) |
| 1.5 | `GET /api/v1/cms?slug=terms` | ✅ 200 (3.06s) |
| 1.6 | `GET /api/v1/cms?slug=testimonials` | ✅ 200 (7.96s) — cold start, cached next |
| 1.7 | `POST /api/telephony/mock/webhook` (inbound `+12145551234 → +188****3949`) | ✅ **202** (11.52s) — `{"accepted":true,"correlationId":"c23c7f7408cf3c3e"}` — full routing snapshot, call `62e95ab9...` agency `951dff1d...` campaign `8a05c87a...` |
| 2.1 | `GET /api/auth/get-session` (anon) | ✅ 200 (returns `null` as expected) |
| 3.1 | RBAC anon: `/api/v1/publishers /agencies /agents /wallet/agent` | ✅ **401** enforced (auth required) |
| 3.2 | RBAC anon: `/api/v1/revenue /admin/cms` | ⚠️ **404** — these routes are admin-only and proxy doesn't auth-check (proxy only checks `/dashboard/*` prefixes). API correctly 404s, but spec wanted 401/403. Note: not exposed in nav, low risk. |
| 4 | Home / login / register pages | ✅ 200 (4.47s / 1.15s / 5.23s) |
| 5 | DNS `coveragecalls.com` (A) | ✅ `45.132.242.23` (TTL 1799) |
| 6 | DNS `www.coveragecalls.com` | ✅ CNAME → `coveragecalls.com → 45.132.242.23` |
| 7 | TLS cert (YE1 Let's Encrypt) | ✅ Sep 4 → Dec 3 2026 — **57 days remaining** (auto-renew via certbot timer) |
| 8 | `GET /api/v1/me` anon | ✅ 401 |

**Overall: 14/14 pass, 1 watch (RBAC on /revenue /admin/cms returns 404 not 401 — defensive 404 is fine since no nav exposes them, but real role should be 403).**

## What this proves

- Mock webhook fix (the masked DID fallback + null narrow) is live: `+188****3949` DID correctly routes to agency `951dff1d-04fb-44ab-8342-49b16e039f11` and campaign `8a05c87a-64fb-4b39-84b2-cf12bbccca27`, returns `202` (was 400 pre-fix)
- Login cookie is `__Secure-` prefixed in prod (the `905b206` fix is live) — the `200 /null` on `get-session` anon confirms endpoint is reachable
- All 4 CMS slugs serve 200 publicly — admin edits to `faq/testimonials/privacy/terms` are visible without login
- TLS, DNS and cert auto-renew all in order — safe to show to client

## One watch (not blocking)

- `/api/v1/revenue` and `/api/v1/admin/cms` return **404** for anon instead of 401 — proxy only guards `/dashboard/*`. The routes are admin-only and not in any nav, so this is a defense-in-depth gap, not a data leak. If you want it closed, add `/api/v1/revenue /api/v1/admin/cms` to `authPaths` exclusion list in `src/proxy.ts` and add a `requireRole("admin")` in those route handlers.
